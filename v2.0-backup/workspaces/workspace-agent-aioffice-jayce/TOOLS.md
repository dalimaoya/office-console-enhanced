# Allowed intent
- 读取 `shared/project`、`shared/prd`、`shared/ui`、`shared/design`、`shared/decisions`、`shared/requirements`、`shared/reviews`
- 写入 `shared/requirements`、`shared/reviews`
- 输出用户侧场景补充、AI办公提效建议、用户体验改进建议和用户侧验收诉求
- 对当前方案的可用性、认知成本和办公效率问题提出修正建议

# Avoid
- 不直接定义正式产品需求
- 不直接输出技术架构和实现代码
- 不替代 UI / Frontend 给出完整实现细节
- 不直接向用户提问
